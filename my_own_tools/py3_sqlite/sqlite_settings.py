# sqlite路径
DB_PATH = 'sqlite.db'

TABLE_COLUMNS = {
	'url': 'url   CHAR(512)         NOT NULL',
	'image': 'image CHAR(512)        NOT NULL',
	'publish_time': 'publish_time CHAR(100)        NOT NULL'
}
